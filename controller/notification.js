
exports.notifyUsers = function(post) {
    console.log('post notification', post);
    // push notification to db
    // use socket io to notify users that have the 'recievenotifications flag enabled'
    // from the following users list of this posts user_id
};
